# The Governor — runtime dependencies
# Pinned to the versions the 16/16 acceptance suite is verified against.
langgraph==1.2.10
langgraph-checkpoint-sqlite==3.1.1
# For the brain (any OpenAI-compatible endpoint — the platform gateway by default). Only used when BRAIN_API_KEY / DEEPSEEK_API_KEY is set.
openai>=1.30,<2
# Deep tracing (Article VII.3). Only active when LANGSMITH_TRACING=true.
langsmith>=0.1
