The Vault — an internal document service.

Log in, read a document, list the directory if you are an admin. Nothing here is
real: the accounts are fake, the documents are fixtures, and the whole service runs
in one process with no socket and no credential.

It exists to be broken, in a place where breaking it costs nothing.
